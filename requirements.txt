# Python dependencies for the notebooks/scripts.
# Spark, Delta, and the Foundation Model API endpoints are provided by the Databricks runtime.
pypdf
typing_extensions>=4.12.0
regex
mlflow
databricks-vectorsearch
